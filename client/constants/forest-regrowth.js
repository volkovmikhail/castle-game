/** Интервал между попытками вырастить одно новое дерево в мире (мс). */
export const TREE_REGROW_INTERVAL_MS = 15_000;

/**
 * Дерево не ставится ближе к зданию, чем на столько клеток по метрике Чебышёва
 * (соседние по стороне и по диагонали к любой клетке здания запрещены).
 */
export const TREE_REGROW_BUILDING_BUFFER_TILES = 1;
